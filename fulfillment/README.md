# Fulfillment

The initial fulfillment path will accept an exact local snapshot, run deterministic checks,
perform QA, and return receipt JSON under the documented deterministic serialization
profile. It must not import or execute artifact content.

