# Next action

Convert any open publisher proposal into a genuine external integration while continuing
the same carefully targeted publisher channel after the daily new-contact cap resets. The immutable fetcher, no-egress worker,
constrained signer, durable ledger, and fake-provider sandbox are complete. Next implement the
reviewed Stripe adapter and offline-root paid-receipt lifecycle are now implemented and tested,
including one real unpaid Stripe test Checkout. Next connect the adapter to authenticated order
creation and an exact-body webhook route behind managed TLS/ingress, then provision the
production signer and kernel-quota fetch volume. The authenticated tenant/order/result API is
still sandbox-proven but not externally deployed. Count only independently
attributable external integrations, requests, and settled payments; owned demo, sandbox,
CI, monitoring, and smoke traffic remain excluded.
