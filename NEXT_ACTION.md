# Next action

Convert either open publisher proposal into a genuine external integration while continuing
the same carefully targeted publisher channel. The immutable fetcher, no-egress worker,
constrained signer, durable ledger, and fake-provider sandbox are complete. Next implement the
authenticated delivery-token API and real Stripe Checkout/webhook/reconciliation adapter,
then reduce payment activation to the minimum owner credential action. Count only independently
attributable external integrations, requests, and settled payments; owned demo, sandbox,
CI, monitoring, and smoke traffic remain excluded.
