---
name: valid-skill
description: Reads a supplied text file and returns a normalized copy. Use for deterministic plain-text normalization tasks.
license: Apache-2.0
compatibility: Requires Python 3.11 or newer; no network access is needed.
metadata:
  author: capabilityproof-test
  version: "1.0"
---

# Valid skill fixture

Read [the guide](references/guide.md), then run `scripts/normalize.py` with an input
path and an output path inside the current workspace.
