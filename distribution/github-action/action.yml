name: VouchSpec publisher CI receipt draft
description: Bind a non-executing Agent Skill receipt draft to the exact publisher repository commit and workflow context.
inputs:
  skill-path:
    description: Relative path below GITHUB_WORKSPACE to the Agent Skill directory.
    required: true
outputs:
  receipt-path:
    description: Absolute path to the exact receipt draft that the publisher should attest.
    value: ${{ steps.paths.outputs.receipt-path }}
  ci-request-path:
    description: Absolute path to workflow/commit binding metadata that the publisher should attest.
    value: ${{ steps.paths.outputs.ci-request-path }}
  structure-status:
    description: Structural profile result from the receipt (`pass` or `fail`).
    value: ${{ steps.paths.outputs.structure-status }}
  decision-status:
    description: Receipt decision status; a reported structural failure remains evidence.
    value: ${{ steps.paths.outputs.decision-status }}
runs:
  using: composite
  steps:
    - name: Install hash-locked VouchSpec runtime
      shell: bash
      env:
        VOUCHSPEC_ACTION_PATH: ${{ github.action_path }}
      run: |
        set -euo pipefail
        python -c 'import sys; assert sys.version_info[:2] == (3, 11), "VouchSpec publisher action requires Python 3.11"'
        python -m venv "$RUNNER_TEMP/vouchspec-action-venv"
        "$RUNNER_TEMP/vouchspec-action-venv/bin/python" -m pip install \
          --disable-pip-version-check \
          --only-binary=:all: \
          --require-hashes \
          -r "$VOUCHSPEC_ACTION_PATH/requirements-linux-x86_64-py311.lock"
        echo "VOUCHSPEC_PYTHON=$RUNNER_TEMP/vouchspec-action-venv/bin/python" >> "$GITHUB_ENV"
        echo "VOUCHSPEC_SOURCE=$VOUCHSPEC_ACTION_PATH/../../src" >> "$GITHUB_ENV"
    - name: Verify exact checkout context
      shell: bash
      env:
        VOUCHSPEC_EXPECTED_COMMIT: ${{ github.sha }}
      run: |
        set -euo pipefail
        actual_commit="$(git -C "$GITHUB_WORKSPACE" rev-parse HEAD)"
        if [[ "$actual_commit" != "$VOUCHSPEC_EXPECTED_COMMIT" ]]; then
          echo "VouchSpec requires the checked-out HEAD to equal GITHUB_SHA." >&2
          exit 1
        fi
        if [[ -n "$(git -C "$GITHUB_WORKSPACE" status --porcelain=v1 --untracked-files=all)" ]]; then
          echo "VouchSpec requires a clean checkout. Run it before build steps that modify the workspace." >&2
          exit 1
        fi
    - name: Generate exact Git-bound receipt draft
      shell: bash
      env:
        VOUCHSPEC_SKILL_PATH: ${{ inputs.skill-path }}
      run: |
        set -euo pipefail
        export PYTHONPATH="$VOUCHSPEC_SOURCE"
        mkdir -p "$RUNNER_TEMP/vouchspec-publisher-ci"
        set +e
        "$VOUCHSPEC_PYTHON" -m capabilityproof inspect-git "$GITHUB_WORKSPACE/$VOUCHSPEC_SKILL_PATH" \
          --repository-root "$GITHUB_WORKSPACE" \
          --compact \
          --output "$RUNNER_TEMP/vouchspec-publisher-ci/receipt.json"
        inspection_status=$?
        set -e
        case "$inspection_status" in
          0|2) ;;
          *)
            echo "VouchSpec inspection failed operationally with exit code $inspection_status." >&2
            exit "$inspection_status"
            ;;
        esac
        test -s "$RUNNER_TEMP/vouchspec-publisher-ci/receipt.json"
    - name: Record workflow and receipt binding
      id: paths
      shell: bash
      env:
        VOUCHSPEC_REPOSITORY: ${{ github.repository }}
        VOUCHSPEC_COMMIT: ${{ github.sha }}
        VOUCHSPEC_WORKFLOW_REF: ${{ github.workflow_ref }}
        VOUCHSPEC_WORKFLOW: ${{ github.workflow }}
        VOUCHSPEC_RUN_ID: ${{ github.run_id }}
        VOUCHSPEC_RUN_ATTEMPT: ${{ github.run_attempt }}
        VOUCHSPEC_ACTION_REPOSITORY: ${{ github.action_repository }}
        VOUCHSPEC_ACTION_REF: ${{ github.action_ref }}
      run: |
        set -euo pipefail
        python - <<'PY'
        import hashlib
        import json
        import os
        from pathlib import Path

        root = Path(os.environ["RUNNER_TEMP"]) / "vouchspec-publisher-ci"
        receipt = root / "receipt.json"
        receipt_value = json.loads(receipt.read_text(encoding="utf-8"))
        statement = {
            "schema_version": "1.0.0",
            "profile": "vouchspec-publisher-ci-request-v1",
            "repository": os.environ["VOUCHSPEC_REPOSITORY"],
            "commit": os.environ["VOUCHSPEC_COMMIT"],
            "workflow_ref": os.environ["VOUCHSPEC_WORKFLOW_REF"],
            "workflow": os.environ["VOUCHSPEC_WORKFLOW"],
            "run_id": os.environ["VOUCHSPEC_RUN_ID"],
            "run_attempt": os.environ["VOUCHSPEC_RUN_ATTEMPT"],
            "action_repository": os.environ["VOUCHSPEC_ACTION_REPOSITORY"],
            "action_ref": os.environ["VOUCHSPEC_ACTION_REF"],
            "receipt_sha256": hashlib.sha256(receipt.read_bytes()).hexdigest(),
        }
        request = root / "publisher-ci-request.json"
        request.write_text(json.dumps(statement, sort_keys=True, separators=(",", ":")) + "\n", encoding="utf-8", newline="\n")
        output = Path(os.environ["GITHUB_OUTPUT"])
        with output.open("a", encoding="utf-8", newline="\n") as stream:
            stream.write(f"receipt-path={receipt}\n")
            stream.write(f"ci-request-path={request}\n")
            stream.write(f"structure-status={receipt_value['format_validation']['status']}\n")
            stream.write(f"decision-status={receipt_value['decision']['status']}\n")
        PY
